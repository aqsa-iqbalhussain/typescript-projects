// for each
var num1 = [2, 3, 6, 4, 7, 8, 2];
num1.forEach(function (number) {
    console.log(number * 2);
});
// map
var num2 = num1.map(function (number) { return number * 2; });
console.log(num2);
// for each 
// return the elements (for list)
// map
// return the elements(for array)
